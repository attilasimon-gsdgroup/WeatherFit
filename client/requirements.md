## Packages
date-fns | For formatting dates and days of the week

## Notes
Weather data is fetched directly from Open-Meteo API (client-side)
Backend /api/locations is used for storing favorite cities
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
